import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    flexContainerMain: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },

    MapContainer:
    {
        flex: 1,
        width: '100%'
    },
});

export { styles };